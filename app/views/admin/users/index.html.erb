<div class="container-fluid">
 <div class="row">
  <h3 class="sub-header"><%= t "views.admin.users.index.user_list" %></h3>
   <%= form_tag admin_users_path, method: :get do %>
     <%= text_field_tag :search, params[:search] %>
     <%= submit_tag t("garena.search"), name: nil %>
   <% end %>
    <%= link_to t("garena.excel"), admin_users_path(format: "xls"), class: "pull-right" %>
    <%= link_to new_admin_user_path do %>
      <span><i class="fa fa-plus fa-2px"></i><%= t "views.admin.users.index.new_user" %></span>
    <% end %>
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th><%= t "garena.name" %></th>
            <th><%= t "garena.email" %></th>
            <th><%= t "garena.activated" %></th>
            <th><%= t "garena.create_at" %></th>
            <th><%= t "garena.role" %></th>
            <th><%= t "garena.edit" %></th>
            <th><%= t "garena.delete" %></th>
          </tr>
        </thead>
        <tbody>
          <%= render partial: "user", collection: @users %>
        </tbody>
      </table>
      <%= paginate @users, theme: "twitter-bootstrap-3" %>
    </div>
  </div>
</div>
